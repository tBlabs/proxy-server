import { MessageBus } from "../MessageBus/MessageBus";

describe('', () =>
{
    it('', () =>
    {
        const sut = new TransCorder(new TransCorderConnector(...));

        const photo = sut.TakePicture();

    });
});